## 2026-06-12 工作记录

### 店员角色功能增强

1. **桌位查询支持按容纳人数筛选**
   - `StaffService.listTables()` 增加 `capacity` 参数
   - `StaffController.listTables()` 增加 `capacity` 请求参数
   - `staff.ts fetchTables()` 增加 `capacity` 参数
   - `TableCatStatusView.vue` 新增人数筛选下拉框（2人/4人/6人）

2. **猫咪性别中文显示**
   - `TableCatStatusView.vue` 增加 `translateGender()` 函数
   - MALE → 公，FEMALE → 母，UNKNOWN → 未知
