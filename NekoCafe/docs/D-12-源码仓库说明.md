# D-12 源码仓库说明

本源码仓库位于 `NekoCafe/`，包含后端、前端、数据库迁移、部署说明和测试目录。

## 30 分钟启动路径

1. 准备 JDK 17、Maven、Node.js。
2. 复制 `.env.example` 为 `.env` 并填写远程 MySQL 配置。
3. 启动后端：`cd backend && mvn spring-boot:run`。
4. 启动前端：`cd frontend && npm install && npm run dev`。
5. 验证 `/actuator/health`、Swagger UI 和前端页面。
